{
  "globalEnv": ["VITE_PUBLIC_CHAIN_ID", "VITE_PUBLIC_INFURA_API_KEY"],
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "lint": {
      "outputs": []
    },
    "local": {
      "cache": false
    },
    "dev": {
      "cache": false
    }
  }
}
